services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: medical_clinic
      POSTGRES_USER: medical_clinic
      POSTGRES_PASSWORD: medical_clinic
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: